# Copy to .env.local and fill in. Email/password auth needs MongoDB running.
MONGODB_URI=mongodb://localhost:27017/lumiere
# Any long random string (e.g. `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`)
JWT_SECRET=replace-with-a-long-random-string
